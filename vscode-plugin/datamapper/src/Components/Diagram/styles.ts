import { makeStyles } from '@mui/styles';
import { createStyles } from '@mui/styles';

export const canvastyles = makeStyles(()=> createStyles({
    canvas : {
        height: '500px',
        width: '100%',
        textAlign:'center',
    }
}))